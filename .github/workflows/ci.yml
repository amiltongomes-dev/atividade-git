name: CI Básico

on:
  push:
    branches:
      - master

jobs:
  verificar-projeto:
    runs-on: ubuntu-latest

    steps:
      - name: baixar codigo
        uses: actions/checkout@v4
        
      - name: verificar arquivos
        run: 
          echo "Projeto recebido"
          ls -la

      - name: Finalizado
        run: echo "Pipeline executado dom sucesso!"